# Frontend (Angular)

This is a minimal Angular 16+ app.

Commands:

- Install dependencies: `npm install`
- Start development server: `npm start` (runs `ng serve --host 0.0.0.0 --port 4200`)
- Build: `npm run build`

If `ng` is not installed globally you can run it via `npx ng <command>`.
